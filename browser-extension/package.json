{
  "name": "applyease-browser-extension",
  "private": true,
  "type": "module",
  "scripts": {"build": "tsc --noEmit && vite build --config vite.config.ts", "test": "vitest run"},
  "devDependencies": {"jsdom": "latest", "typescript": "latest", "vite": "latest", "vitest": "latest"}
}
